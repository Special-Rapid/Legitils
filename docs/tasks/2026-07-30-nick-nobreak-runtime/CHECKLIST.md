# Checklist

- [ ] Java 8 `:mod:test` and `verifyBootstrapArtifacts` pass.
- [ ] Lunar is fully quit and restarted after replacing the JAR.
- [ ] `.l nickdetect on` and `.l dev on` are confirmed by `.l status`.
- [ ] A known nick receives Legitils' red `[NICK]` in the name-tag/Tab path.
- [ ] Terminal log contains the Legitils Nick observation line.
- [ ] A normal break produces the one-time NoBreakDelay controller-hook log.
- [ ] F3+T still produces no NoBreakDelay alert.
