# Goal

Create a public GitHub repository named `Legitils` containing only first-party source, documentation, and licensed project assets.

## Stop conditions

- `sample/`, local Gradle caches, generated output, logs, and unrelated MirrorProxy assets are absent from the initial commit.
- The root README documents the alpha status, safety boundary, build command, and manual validation limit.
- Build and publication checks complete, followed by a final review with no unresolved blocking issue.
